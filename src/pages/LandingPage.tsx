import { Link } from 'react-router-dom';
import { ShieldCheck, Code2, GitCompareArrows, Zap, Wallet, FileBarChart } from 'lucide-react';
import { Button } from '@/components/ui/Button';
import { Card, CardContent } from '@/components/ui/Card';

const BENEFITS = [
  { icon: Wallet, title: 'One integration', body: 'Accept configured digital payment methods through a single GiantPay integration.' },
  { icon: ShieldCheck, title: 'Clear transaction evidence', body: 'Every payment carries a trusted backend status, full event history and reconciliation state.' },
  { icon: GitCompareArrows, title: 'Reconciliation built in', body: 'Match transactions against provider and settlement records without manual spreadsheets.' },
  { icon: FileBarChart, title: 'Reporting that holds up', body: 'Export transaction, fee, refund and settlement reports generated by the backend, not the browser.' },
];

const FAQS = [
  {
    q: 'Which payment methods can I accept?',
    a: 'Available channels depend on what is configured for your merchant account and shown at checkout — GiantPay does not claim support for a channel until it is enabled for you.',
  },
  {
    q: 'How do I get started?',
    a: 'Register an account, complete KYC/KYB, and GiantPay reviews your application before granting sandbox and later production access.',
  },
  {
    q: 'Can I test before going live?',
    a: 'Yes — every merchant gets a sandbox environment for building and testing an integration before production activation.',
  },
];

export default function LandingPage() {
  return (
    <>
      <section className="border-b border-[var(--color-neutral-200)] bg-[var(--color-navy-950)]">
        <div className="mx-auto max-w-6xl px-4 py-20 text-center sm:px-6">
          <h1 className="mx-auto max-w-3xl text-[length:var(--text-display)] font-semibold text-white">
            Unified payments. One integration.
          </h1>
          <p className="mx-auto mt-4 max-w-2xl text-[length:var(--text-h4)] font-normal text-[var(--color-navy-100)]">
            GiantPay helps businesses and digital platforms in Malawi accept and manage configured digital payment
            methods through one secure integration, with clear transaction evidence, developer tools, reconciliation
            and reporting.
          </p>
          <div className="mt-8 flex flex-wrap justify-center gap-3">
            <Button asChild size="lg">
              <Link to="/register">Get Started</Link>
            </Button>
            <Button asChild size="lg" variant="secondary">
              <Link to="/developers">Explore Developer Tools</Link>
            </Button>
          </div>
        </div>
      </section>

      <section className="mx-auto max-w-6xl px-4 py-16 sm:px-6">
        <h2 className="text-center text-[length:var(--text-h1)] font-semibold text-[var(--color-navy-900)]">How GiantPay works</h2>
        <ol className="mx-auto mt-8 grid max-w-4xl gap-6 sm:grid-cols-3">
          {[
            { step: '1', title: 'Register & verify', body: 'Submit your business and KYC/KYB details for review.' },
            { step: '2', title: 'Integrate & test', body: 'Build against sandbox using payment links or the API.' },
            { step: '3', title: 'Go live', body: 'Move to production once your application is approved.' },
          ].map((item) => (
            <li key={item.step} className="rounded-[var(--radius-md)] border border-[var(--color-neutral-200)] p-5 text-center">
              <span className="mx-auto flex h-9 w-9 items-center justify-center rounded-full bg-[var(--color-blue-100)] text-[length:var(--text-body)] font-semibold text-[var(--color-blue-600)]">
                {item.step}
              </span>
              <p className="mt-3 font-semibold text-[var(--color-navy-900)]">{item.title}</p>
              <p className="mt-1 text-[length:var(--text-label)] text-[var(--color-neutral-600)]">{item.body}</p>
            </li>
          ))}
        </ol>
      </section>

      <section className="bg-[var(--color-neutral-50)] py-16">
        <div className="mx-auto max-w-6xl px-4 sm:px-6">
          <h2 className="text-center text-[length:var(--text-h1)] font-semibold text-[var(--color-navy-900)]">Built for merchants and developers</h2>
          <div className="mt-8 grid gap-4 sm:grid-cols-2">
            {BENEFITS.map((b) => (
              <Card key={b.title}>
                <CardContent className="flex gap-4">
                  <b.icon className="h-6 w-6 shrink-0 text-[var(--color-blue-600)]" aria-hidden="true" />
                  <div>
                    <p className="font-semibold text-[var(--color-navy-900)]">{b.title}</p>
                    <p className="mt-1 text-[length:var(--text-label)] text-[var(--color-neutral-600)]">{b.body}</p>
                  </div>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </section>

      <section className="mx-auto max-w-4xl px-4 py-16 sm:px-6">
        <div className="flex items-center gap-3">
          <Code2 className="h-6 w-6 text-[var(--color-blue-600)]" aria-hidden="true" />
          <h2 className="text-[length:var(--text-h1)] font-semibold text-[var(--color-navy-900)]">Developer experience</h2>
        </div>
        <p className="mt-3 text-[length:var(--text-body)] text-[var(--color-neutral-600)]">
          Sandbox and production API keys, webhooks with delivery history, idempotent requests, and documentation with
          cURL, JavaScript/TypeScript and PHP examples — all built around the same trusted-status model the hosted
          checkout uses.
        </p>
        <Button asChild variant="secondary" className="mt-4">
          <Link to="/developers">View developer documentation</Link>
        </Button>
      </section>

      <section className="border-t border-[var(--color-neutral-200)] py-16">
        <div className="mx-auto max-w-3xl px-4 sm:px-6">
          <div className="flex items-center gap-3">
            <Zap className="h-6 w-6 text-[var(--color-blue-600)]" aria-hidden="true" />
            <h2 className="text-[length:var(--text-h1)] font-semibold text-[var(--color-navy-900)]">Frequently asked questions</h2>
          </div>
          <dl className="mt-6 flex flex-col gap-5">
            {FAQS.map((faq) => (
              <div key={faq.q}>
                <dt className="font-semibold text-[var(--color-navy-900)]">{faq.q}</dt>
                <dd className="mt-1 text-[length:var(--text-body)] text-[var(--color-neutral-600)]">{faq.a}</dd>
              </div>
            ))}
          </dl>
        </div>
      </section>
    </>
  );
}
