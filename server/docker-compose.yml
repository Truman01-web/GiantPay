services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: giantpay
      POSTGRES_USER: giantpay
      POSTGRES_PASSWORD: giantpay
    ports:
      - "5432:5432"
    volumes:
      - giantpay_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U giantpay"]
      interval: 5s
      timeout: 3s
      retries: 10
volumes:
  giantpay_postgres:
